name: Terraform Apply

on:
  push:
    branches:
      - main
    paths:
      - "infra/**"
      - ".github/workflows/terraform-apply.yml"
      - ".github/workflows/reusables/terraform-*.yml"

permissions:
  contents: read
  id-token: write

env:
  TF_WORKING_DIR: infra
  ARM_USE_OIDC: true
  ARM_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
  ARM_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
  ARM_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}

jobs:
  format:
    name: Terraform Format Check
    uses: ./.github/workflows/reusables/terraform-format.yml
    permissions:
      contents: write
    with:
      working_directory: "infra"
      terraform_version: "1.14.3"
      enable_auto_commit: true
    secrets: inherit

  security:
    name: Security Analysis (tfsec)
    uses: ./.github/workflows/reusables/terraform-security.yml
    permissions:
      contents: read
    with:
      working_directory: "infra"
      tfsec_soft_fail: false
    secrets: inherit

  apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: [format, security]
    if: needs.format.result == 'success' && needs.security.result == 'success'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.14.3"

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Terraform Init
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: |
          terraform init

      - name: Terraform Validate
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: |
          terraform validate

      - name: Terraform Plan
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: |
          terraform plan -out=tfplan

      - name: Terraform Apply
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: |
          terraform apply tfplan

      - name: Add deployment summary
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: |
          echo "## 🏗️ Infrastructure Deployment Successful" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Terraform has successfully applied the infrastructure changes." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Outputs" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          terraform output -no-color >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
